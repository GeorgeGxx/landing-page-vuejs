version: '3.9'
services:
  backend:
    container_name: frontend_app
    image: nodeimage
    restart: always
    networks:
      - frontend
    build:
      context: ./
      dockerfile: Dockerfile    
    ports:
      - 80:80

networks:
  frontend:
    name: frontend